import { FilterProbeByStorePipe } from './filter-probe-by-store.pipe';

describe('FilterProbeByStorePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProbeByStorePipe();
    expect(pipe).toBeTruthy();
  });
});
