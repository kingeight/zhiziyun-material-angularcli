import { MyWizardDirective } from './my-wizard.directive';

describe('MyWizardDirective', () => {
  it('should create an instance', () => {
    const directive = new MyWizardDirective();
    expect(directive).toBeTruthy();
  });
});
